paths:
  /api/product/get:
    get:
      operationId: getProduct
      tags:
        - Product
      summary: Get products
      security:
        - ApiKey: []
          ApiSecret: []
      requestBody:
        description: Get products from the database
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FilstersAndOrderBy"
      responses:
        "200":
          $ref: "#/components/responses/successGetProductResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalError"
